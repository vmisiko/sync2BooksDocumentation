{
  "name": "sync2books-docs",
  "version": "1.0.0",
  "description": "Sync2Books API Documentation",
  "scripts": {
    "install": "npx gitbook install",
    "serve": "npx gitbook serve",
    "build": "npx gitbook build"
  },
  "devDependencies": {
    "gitbook-cli": "^2.3.2"
  }
}
